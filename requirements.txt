# Streamlit 앱 실행에 필요한 패키지
streamlit>=1.25.0

# OpenAI SDK (AsyncOpenAI 사용 가능 버전)
openai>=1.3.5

# 비동기 처리 (필요 시)
aiohttp>=3.8.0

# 정규표현식 (표준 라이브러리지만 일부 환경에선 명시적으로 요구되기도 함)
regex

# 기타 유틸 패키지 (선택)
tqdm
